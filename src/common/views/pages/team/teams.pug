extends ../../layout

block content
	.inner
		h1 #{title}
		.teams
			if !user.team
				a(href="/teams/create") Create A team
			if user.team
				pre= user.team
			each team in teams
				.team
					pre #{team}